[build]
builder = "dockerfile"
dockerfilePath = "./API.Lanchonete/Dockerfile"
dockerContext = "./API.Lanchonete"

[deploy]
startCommand = "./API.Lanchonete"
restartPolicyType = "always"
healthcheckPath = "/health"